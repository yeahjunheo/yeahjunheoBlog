version: "2"
sql:
    - engine: "postgresql"
      queries: "internal/db/queires"
      schema: "migrations"
      gen:
        go:
          package: "db"
          out: "internal/db/generated"
          sql_package: "pgx/v5"
          emit_json_tags: true
          emit_pointers_for_null_types: true