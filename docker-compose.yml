version: '3.8'

services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yeahjunheo-blog
    ports:
      - "3000:80"
    restart: unless-stopped
    # Note: This uses static export mode (same as GitHub Pages)
    # For development, use 'npm run dev' directly instead
