---
import type { Tag } from "../lib/api";

interface Props {
  tag: Tag;
}

const { tag } = Astro.props;
---

<a
  href={`/tags/${tag.slug}`}
  class="inline-block border border-border-medium text-text-secondary text-sm px-3 py-1 hover:border-accent hover:text-accent transition-colors"
>
  {tag.name}
</a>
